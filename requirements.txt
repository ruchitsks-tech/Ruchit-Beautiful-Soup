# BSE Earnings Call Transcript Scraper — Dependencies
# Install with: pip install -r requirements.txt

# HTML parsing (html.parser is built into Python — no lxml needed)
beautifulsoup4==4.12.3

# Browser automation (JavaScript-rendered pages)
selenium==4.21.0

# HTTP downloads
requests==2.32.2
